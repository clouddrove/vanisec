apiVersion: v1
kind: Service
metadata:
  name: {{ include "vanisec.fullname" . }}
  labels:
    {{- include "vanisec.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vanisec.selectorLabels" . | nindent 4 }}

